# אלמוג אלון — תיק עבודות

## מה זה
אתר תיק עבודות של אלמוג אלון, ספר. עמוד אחד, סטטי, בעברית (RTL).
הקישור נועד לשבת ב-bio של האינסטגרם — מי שלוחץ רואה את העבודות, את הסיפור ואת דרך יצירת הקשר. אין דומיין, אין שרת, אין בסיס נתונים.

## למי זה מיועד
לקוחות פוטנציאליים שמגיעים מהאינסטגרם, רובם המוחלט מהנייד. הנייד קודם — תמיד.

## הטכנולוגיה
- HTML + CSS + JavaScript בלבד. **בלי framework, בלי build, בלי npm.**
- פונטים: Heebo (עברית + לטינית) ו-Anton (טקסט תצוגה לטיני) מ-Google Fonts.
- מתארח ב-GitHub Pages מהענף `main`, מהשורש.
- כל הנתיבים **יחסיים** (`assets/...`, `css/...`) — האתר יושב בתת-נתיב `/ALMOG-ALON-/`, נתיב מוחלט שמתחיל ב-`/` ישבור אותו.

## מבנה
```
index.html            כל התוכן. מחולק למקטעים עם הערות בעברית.
css/style.css         7 שכבות לפי הסדר: טוקנים, בסיס, פריסה, רכיבים,
                      מקטעים, תנועה, רספונסיבי.
js/app.js             CONTACT בראש הקובץ + IIFE אחת עם כל ההתנהגות.
assets/logo.jpg       פוסטר הלוגו (780×1696) — גיבור ה-HERO.
assets/scene.jpg      איור המספרה (860×1870) — מקטע "על הכיסא", כולל הבבואה.
assets/avatar.jpg     דיוקן חתוך מהפוסטר, לכרטיס הזהות בסיפור.
assets/og.jpg         תמונת התצוגה המקדימה בשיתוף (1200×630).
assets/icon.png       אייקון אפליקציה, assets/favicon.svg אייקון לשונית.
assets/work/*.jpg     8 תמונות עבודות, חתוכות נקי מצילומי מסך של אינסטגרם.
```

## כללי עבודה
- **מקור אמת אחד לכל מספר.** כל צבע, מרווח, גודל טקסט ומהירות אנימציה חיים כטוקן ב-`:root` בראש ה-CSS. לא מכניסים ערך קשיח בגוף הקובץ.
- **פרטי קשר רק ב-`CONTACT`** שבראש `js/app.js`. מהטלפון המקומי נגזרים לבד קישור החיוג (`tel:+972…`), קישור הוואטסאפ והמספר שמוצג; מהכתובת נגזרים קישור ה-Waze והטקסט שמוצג. כפתור שאין לו ערך מסתיר את עצמו אוטומטית.
- מקטע "על הכיסא" הוא **במה תלת-ממדית אמיתית**: `perspective` על `.scene__stage`, `rotateY/rotateX` על הקלף, בבואה הפוכה מתחתיו ורצפת רשת בפרספקטיבה. ההטיה עוקבת אחרי העכבר דרך `--ry`/`--rx` — רק במכשירים עם `hover: hover`. אין כאן תמונת רקע מטושטשת, ואין להחזיר אחת.
- במגע אין `hover`. כל רמז ויזואלי שמסתמך עליו צריך מקבילה ב-`@media (hover:none)` — כמו סימן ההגדלה על כרטיסי הגלריה.
- **RTL אמיתי:** משתמשים ב-`inset-inline-start` / `padding-inline` / `border-inline-start` ולא ב-`left`/`right`. שים לב: ב-RTL ‏`inline-start` = **ימין**.
- `backdrop-filter` על אלמנט הופך אותו לבלוק המכיל של צאצאים ב-`position:fixed`. לכן רקע הניווט יושב על `.nav::before` ולא על `.nav` — אל תחזיר אותו.
- תמונות: תמיד עם `width` ו-`height` בתגית (מונע קפיצות בטעינה) ועם `loading="lazy"` חוץ מהתמונה הראשונה.
- להסתיר אלמנט — התכונה `hidden`. יש כלל גלובלי `[hidden]{display:none !important}` שמנצח את ה-`display` של הרכיבים.
- לכבד `prefers-reduced-motion` בכל אנימציה חדשה.

## טון הכתיבה
עברית פשוטה וחמה, **בגוף ראשון** — אלמוג מדבר על עצמו ("אני", "התחלתי", "אני מספר").
צנוע ואותנטי, **לא שחצן**: בלי סופרלטיבים ("הכי טוב בארץ") ובלי להרחיב על הכישרון שלו.
בלי מחירים שלא נמסרו ובלי כתובת שלא אושרה.

הסיפור: הכדורגל היה החיים שלו מגיל צעיר ולימד אותו משמעת ודיוק; **בגיל 17 פרש**
וחיפש דרך חדשה; התחיל לספר מתוך תשוקה לאסתטיקה ולדיוק של כל שערה; היום מספר
בסניף **שניר ומאיר**. אין בסיפור סעיף "המספריים" — הוסר בכוונה.

## הרצה מקומית
אין שרת פיתוח. פותחים את `index.html` בדפדפן, או מריצים שרת סטטי קטן ונכנסים ל-`http://127.0.0.1:8099`.
הדפדפן נוטה לשמור את ה-CSS בקאש — בבדיקות מוסיפים `?bust=1` לכתובת של הקובץ.

## פרסום
דחיפה ל-`main` מעדכנת את האתר החי תוך דקה-שתיים. ראה `.claude/commands/deploy.md`.
