# מצב נוכחי
## עודכן: 18.09.2026

## סיכום הפרויקט
אתר תיק עבודות של אלמוג אלון, ספר. עמוד אחד סטטי בעברית, בצבעי עמוד הספרים (כחול־לבן־אדום) לפי הפוסטר של הלוגו. הקישור מיועד ל-bio באינסטגרם.

## מה נעשה
- [x] חיתוך 6 תמונות מצילומי מסך של אינסטגרם + ניקוי סמלי ה-UI שנצרבו בהן ("1/2" וסמל ההשתקה)
- [x] יצירת `assets/og.jpg` (תמונת שיתוף) ו-`assets/icon.png` + `favicon.svg`
- [x] בניית האתר: HERO, פס נע, הסיפור (ציר זמן), גלריה עם סינון ומסך מלא, שירותים, "למה אלמוג", צור קשר, פוטר
- [x] בדיקה בדפדפן ב-1440px וב-375px, כולל תפריט נייד, סינון, מסך מלא והחלקה באצבע
- [x] `CLAUDE.md`, `README.md`, `/deploy`

## מה בתכנון
- [ ] המשתמש מעלה ל-GitHub ומפעיל Pages
- [ ] למלא מספר וואטסאפ ב-`CONTACT` שב-`js/app.js` (כרגע ריק — הכפתור מוסתר)
- [ ] להוסיף עוד עבודות לגלריה כשיהיו תמונות באיכות טובה

## החלטות שנלקחו
| החלטה | סיבה |
|--------|------|
| גלריית `column-count` (masonry) ולא גריד עם חיתוך | המשתמש ביקש תמונות שלמות ולא צפופות בנייד |
| רקע הניווט על `.nav::before` | `backdrop-filter` על `.nav` הפך אותו לבלוק המכיל של תפריט ה-`position:fixed` ושבר אותו בנייד |
| `[hidden]{display:none !important}` גלובלי | `.btn` עם `display:inline-flex` ניצח את כלל ברירת המחדל של `hidden` והכפתור לא הסתתר |
| בלי כתובת, טלפון או מחירים | לא נמסרו. לא ממציאים פרטים עסקיים |

## הערות חשובות
- הסיפור נכתב בגוף שלישי ובטון צנוע — **לא להוציא את אלמוג שחצן**. בלי סופרלטיבים.
- המקור לתמונות המקוריות: צילומי מסך מהאינסטגרם `almog.alonn`.
- הדפדפן שומר את ה-CSS בקאש — בבדיקות מוסיפים `?bust=` לכתובת הקובץ.
