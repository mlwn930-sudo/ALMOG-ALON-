# deploy — פרסום האתר

> הפעל כשסיימת שינוי ורוצה שהוא יעלה לאתר החי.

**קלט:** $ARGUMENTS — תיאור קצר של השינוי (אופציונלי). אם לא סופק, נסח בעצמך מתוך ה-diff.

---

## שלבים

1. **בדיקה לפני שליחה**
   - ודא שהאתר נפתח מקומית בלי שגיאות ב-console.
   - בדוק ברוחב נייד (375px) וברוחב מחשב (1440px).
   - ודא שכל נתיב חדש הוא **יחסי** (`assets/...`), לא מתחיל ב-`/`.
   - אם נוספה תמונה — ודא שיש לה `width`, `height`, `alt` ו-`loading="lazy"`.

2. **סטטוס**
   - `git status` — ודא שלא נכנסו קבצים זמניים או צילומי מסך גולמיים.

3. **שליחה**
   ```bash
   git add -A
   git commit -m "<תיאור השינוי>"
   git push
   ```

4. **אימות**
   - חכה דקה-שתיים.
   - פתח את https://mlwn930-sudo.github.io/ALMOG-ALON-/ ובדוק שהשינוי עלה.
   - אם לא — בדוק ב-GitHub תחת Actions שה-build של Pages הצליח.

5. **עדכון זיכרון**
   - עדכן את `.claude/memory/active_context.md`: מה שונה ומה הצעד הבא.

## כללים
- לא דוחפים בלי לבדוק מקומית קודם.
- לא משנים את מבנה התיקיות בלי לעדכן את `CLAUDE.md`.
- אם שם המאגר השתנה — יש לעדכן את `canonical`, `og:url` ו-`og:image` ב-`index.html`.
